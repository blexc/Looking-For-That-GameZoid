/// @description debug
draw_self();
/*
// draw path
draw_set_alpha(0.5);
draw_set_color(c_purple);

var _xtarget = (state == enemy_state_idle) ? xwander : xseen;
var _ytarget = (state == enemy_state_idle) ? ywander : yseen;

// draw path
draw_set_alpha(0.5);
draw_set_color(c_purple);
draw_line(x			, y			, _xtarget		 , _ytarget);
draw_line(x + UNIT	, y			, _xtarget + UNIT, _ytarget);
draw_line(x			, y + UNIT	, _xtarget		 , _ytarget + UNIT);
draw_line(x + UNIT	, y + UNIT	, _xtarget + UNIT, _ytarget + UNIT);
*/
